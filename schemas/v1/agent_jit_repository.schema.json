{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Envelope_Repository",
  "type": "object",
  "properties": {
    "schema": {
      "type": "string",
      "const": "agent_jit.repository"
    },
    "version": {
      "type": "integer",
      "const": 1
    },
    "id": {
      "$ref": "#/$defs/RepositoryId"
    },
    "provenance": {
      "$ref": "#/$defs/Provenance"
    },
    "body": {
      "$ref": "#/$defs/Repository"
    }
  },
  "additionalProperties": false,
  "required": [
    "schema",
    "version",
    "id",
    "provenance",
    "body"
  ],
  "$defs": {
    "Repository": {
      "description": "A Git repository the recorder observes.\n\nIdentity is derived from the Git common directory, so every worktree of one repository maps to\none record while each trajectory still records the worktree it ran in.",
      "type": "object",
      "properties": {
        "git_common_dir": {
          "description": "Absolute path of the Git common directory that defines this repository's identity.",
          "type": "string"
        },
        "identity_digest": {
          "description": "Digest of the canonical identity inputs, stable across worktrees and checkouts.",
          "$ref": "#/$defs/Digest"
        },
        "label": {
          "description": "Human-facing name, used only in reports.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "git_common_dir",
        "identity_digest",
        "label"
      ]
    },
    "Digest": {
      "description": "BLAKE3 digest of a canonical JSON byte string, lowercase hex.",
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "RepositoryId": {
      "description": "Identifier for a Repository record.",
      "type": "string",
      "pattern": "^rep_[0-9A-HJKMNP-TV-Z]{26}$"
    },
    "Provenance": {
      "description": "Provenance recorded alongside every envelope.",
      "type": "object",
      "properties": {
        "produced_by": {
          "description": "Name and version of the component that produced the record, e.g. `agent-jit/0.1.0`.",
          "type": "string"
        },
        "source": {
          "description": "How the record entered the system.",
          "$ref": "#/$defs/ProvenanceSource"
        },
        "recorded_at_unix_ms": {
          "description": "Wall-clock time the record was produced. Volatile: never part of a digest.",
          "type": "integer",
          "format": "int64"
        },
        "parents": {
          "description": "Digests of the records this one was derived from, in a stable order.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Digest"
          },
          "default": []
        }
      },
      "additionalProperties": false,
      "required": [
        "produced_by",
        "source",
        "recorded_at_unix_ms"
      ]
    },
    "ProvenanceSource": {
      "description": "How a record entered the system.",
      "oneOf": [
        {
          "description": "Captured live from agent hooks.",
          "type": "string",
          "const": "recorded"
        },
        {
          "description": "Imported from an exact-shape historical record.",
          "type": "string",
          "const": "imported"
        },
        {
          "description": "Derived from other records already in the store.",
          "type": "string",
          "const": "derived"
        },
        {
          "description": "Confirmed by a human operator.",
          "type": "string",
          "const": "confirmed"
        }
      ]
    }
  }
}
